{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "problem": "NITRO_PRESET=deno-server nuxt build && deno run --unstable --allow-net --allow-read --allow-env .output/server/index.mjs",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.11.2",
    "vue": "^3.4.27",
    "vue-router": "^4.3.2",
    "@vue-email/nuxt": "^0.8.19"
  }
}
